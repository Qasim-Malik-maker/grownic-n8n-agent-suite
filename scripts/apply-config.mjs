import fs from 'node:fs';import path from 'node:path';import {spawnSync} from 'node:child_process';import {root,validateConfig} from './configure.mjs';
const c=validateConfig(JSON.parse(fs.readFileSync(path.join(root,'config/client.json'))));const encoded=Buffer.from(JSON.stringify(c)).toString('base64');const sql="INSERT INTO grownic.settings(config) VALUES(convert_from(decode('"+encoded+"','base64'),'UTF8')::jsonb) ON CONFLICT(id) DO UPDATE SET config=excluded.config;";const r=spawnSync('docker',['compose','exec','-T','postgres','psql','-U','bootstrap','-d','grownic','-v','ON_ERROR_STOP=1'],{cwd:root,input:sql,encoding:'utf8'});if(r.status!==0)throw new Error(r.stderr||'docker compose failed');console.log('Client configuration applied: '+c.mode);
